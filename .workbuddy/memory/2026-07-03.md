# 2026-07-03 工作日志

## Web 端接入设计方案

- 完成 Commit 项目 Web 端接入的完整规划文档：`docs/WEB_INTEGRATION_DESIGN.md` (v1.1)
- 深入分析了现有 Flutter 架构（Clean Architecture: Domain/Application/Data/Presentation 四层）
- 确认了关键决策：独立 React Web 项目 + Dexie.js (IndexedDB) + Monorepo 子目录
- 技术栈：React 18 + TypeScript + Vite + Tailwind CSS 4 + Zustand + Radix UI + Dexie.js + vite-plugin-pwa
- Git Graph 方案：ReactFlow（内置 pan/zoom + 自定义节点/连线），优于 D3.js/vis-network/Canvas 手写
- 热力图方案：CSS Grid + Tailwind（纯 CSS，无需 Canvas）
- 架构预留同步扩展：Repository 接口模式，未来引入后端时 DI 一行切换 Dexie→Remote 实现
- 数据兼容性：JSON 导出/导入格式与 Flutter 端 DataExportService 完全一致
- 设计 Token 精准映射：colors.dart → Tailwind colors，typography.dart → Tailwind fontSize + letterSpacing，dimensions.dart → Tailwind spacing + borderRadius

## v1.1 更新：移动端适配策略

- 用户要求 Web 版必须适配手机版本
- 重写 §8 响应式布局策略，从 4 行表格扩展为完整移动端适配方案（10 个子章节）
- Mobile-First 设计哲学：默认样式面向移动端，min-width 逐级增强
- 断点严格对齐 Flutter dimensions.dart：mobile(0)/tablet(768)/laptop(1024)/desktop(1280)/desktop-xl(1440)
- 导航切换沿用 AppScaffold 840px 断点：≥840px 侧边导航，<840px 底部导航
- 逐页适配方案：首页/仓库详情/任务详情/热力图/Git Graph/搜索/设置 各有独立适配表
- Git Graph 移动端：ReactFlow pinch-to-zoom + 节点命中区域扩展到 44px + 默认缩放 0.8x
- 热力图移动端：水平滚动 + 2×2 统计卡片 + 格子缩小到 10px
- 触摸交互规范：44px 最小触摸目标 + 左滑/右滑/下拉刷新手势
- 移动端 UI 模式映射：ModalBottomSheet / PullToRefresh / FAB / 左滑删除
- PWA 支持：vite-plugin-pwa + manifest.json + Service Worker + iOS SafeArea 适配
- 移动端性能优化：路由级 lazy loading / prefers-reduced-motion / 虚拟列表 / touch-action
- 新增依赖：vite-plugin-pwa / @tanstack/react-virtual
- 新增组件：bottom-sheet / pull-to-refresh / fab / safe-area / layout 目录
- 新增 hooks：use-is-desktop / use-is-mobile / use-swipe / use-breakpoint
- 更新开发计划：各阶段加入移动端任务 + 移动端专项走查
- 更新风险表：新增 Git Graph 移动端交互 / 小屏溢出 / iOS SafeArea / PWA 兼容性风险
